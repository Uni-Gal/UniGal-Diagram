<node>
  <identification>
    <name>
      //内部标识用的名字
    </name>
    <uuid>
      //和内部标识的名字至少有一件
    </uuid>
  </identification>
  <description>
    //显示出来的描述或标题等名字
  </description>
  <type>
    <start>
      //是第几周目
    </start>
    <end>
      //是哪个结局
    </end>
    <label>
      //可以不填，代表是普通的节点
    </label>
    <switch>
      <condiation>
        //填写这个分支的进入条件,顺序和nextlist里面一致
      </condiation>
      <condiation>
        //填写这个分支的进入条件,顺序和nextlist里面一致
      </condiation>
    </switch>
    <action operation="add"><!--此外还有sub,multiply,divide,square,not-->
      <var>
        //变量名，类型只有unsigned long long int，包括bool在内也是int。需要浮点的自行除100或者更多。
      </var>
      <num>
        //操作数量
      </num>
    </action>
  </type>
  <loop>
    //如何描述循环，是否要展开循环，是否允许循环，是一个问题
    //建议这里只可以写true与false
  </loop>
  <nextlist>
    <next>
      //下一节点名字/uuid
    </next>
    <next>
      //下一节点名字/uuid,如果该节点type不为switch则只看第一个
    </next>
	//只有end才可以没有next
  </nextlist>
  <sourcelist>
    <source>
      //上一节点名字/uuid
    </source>
    <source>
      //上一节点名字/uuid,如果loop为false则只看第一个
    </source>
	//只有start才可以没有source
  </sourcelist>
  <addtional><!--以下均为选加，也可以需要啥加啥-->
    <comment>
      /**/
    </comment>
    <image>
      
    </image>
    <default_status>
      
    </default_status>
  </addtional>
</node>
